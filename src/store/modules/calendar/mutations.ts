import { MutationTree } from 'vuex';

import { State } from './index';

const mutations: MutationTree<State> = {
  // setAll(state, items: Faq[]) {
  //   state.items = items.map((i: Faq) => new Faq(i));
  // },
};

export default mutations;
