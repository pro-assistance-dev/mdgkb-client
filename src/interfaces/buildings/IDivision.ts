export default interface IDivision {
  id?: string;
  name: string;
  info?: string;
  phone?: string;
  email?: string;
  address?: string;
  floorId?: string;
  buildingId?: string;
  slug?: string;
}
