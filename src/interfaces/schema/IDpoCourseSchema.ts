export default interface IDpoCourseSchema {
  tableName: string;
  key: string;
  id: string;
  name: string;
  isNmo: string;
  hours: string;
  teacherId: string;
  listeners: string;
  start: string;
}
